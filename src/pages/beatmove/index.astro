---
// Import Tailwind CSS
import '../../styles/global.css';
import { Picture, Image } from 'astro:assets';
import heroImage from './hero.png';
import aboutImage from './about.png';
import persona1Image from './persona1.png';
import persona2Image from './persona2.png';
import persona3Image from './persona3.png';
import persona4Image from './persona4.png';
import syncSvg from './sync5.svg';
// Add this import at the top of your file
import tourImage11 from './tour-image-11.jpg';
import tourImage12 from './tour-image-12.jpg';
import beatmoveActualImage from './beatmoveActual.jpg';
import experienceImage from './experience.png';
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>BeatMove（ビートムーブ） - 運動しながら音楽を創る新体験</title>
    <style>
      @keyframes gradientBG {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }
      .header-bg {
        background: linear-gradient(
          90deg,
          rgba(6, 182, 212, 0.3),
          /* cyan */ rgba(59, 130, 246, 0.3),
          /* blue */ rgba(236, 72, 153, 0.3) /* pink */
        );
        background-size: 200% 200%;
        animation: gradientBG 15s ease infinite;
      }
      .glass-effect {
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        background-color: rgba(255, 255, 255, 0.1);
      }
      /* Add this to your existing styles */
      body {
        margin: 0;
        padding: 0;
      }

      #pageOverlay {
        background: linear-gradient(
          to bottom,
          rgba(0, 0, 0, 0) 0%,
          rgba(59, 130, 246, 0.3) 50%,
          rgba(236, 72, 153, 0.5) 100%
        );
        mix-blend-mode: overlay;
      }
    </style>
    <script src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"></script>
  </head>
  <body class="bg-gray-900 text-white font-sans">
    <!-- Add this right after the opening <body> tag -->
    <div
      id="pageOverlay"
      class="fixed inset-0 z-40 pointer-events-none transition-opacity duration-300 ease-in-out opacity-0 mix-blend-overlay"
    >
    </div>

    <!-- ヘッダー -->
    <header class="fixed top-0 left-0 right-0 z-50 transition-transform duration-300 ease-in-out">
      <div class="header-bg glass-effect">
        <div class="container mx-auto px-6 py-4">
          <div class="flex justify-between items-center">
            <div class="text-2xl font-bold text-white">BeatMove</div>
            <nav class="space-x-4">
              <a href="#vision" class="text-white hover:text-pink-300 transition duration-300">ビジョン</a>
              <a href="#revolution" class="text-white hover:text-pink-300 transition duration-300">革新</a>
              <a href="#contact" class="text-white hover:text-pink-300 transition duration-300">お問い合わせ</a>
            </nav>
          </div>
        </div>
      </div>
    </header>

    <!-- ヒーローセクション -->
    <section class="relative h-screen flex items-center justify-center overflow-hidden">
      <Picture
        src={heroImage}
        formats={['jpg']}
        alt="BeatMove Hero Image"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-black opacity-30"></div>
      <div class="container mx-auto px-6 relative z-10 text-center">
        <h1 class="text-8xl font-bold mb-8 leading-tight">
          <span class="text-white">音楽を</span><span
            class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-pink-600">創る</span
          ><span class="text-white">。</span><br />
          <span class="text-white">身体が</span><span
            class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-500">奏でる</span
          ><span class="text-white">。</span>
        </h1>
        <p class="text-4xl mb-16 text-shadow-lg">
          BeatMove - 世界初、フィットネスの概念を塗り替える、<br />新時代のエンターテインメント
        </p>
        <a
          href="#contact"
          class="bg-gradient-to-r from-pink-500 to-purple-500 text-white px-16 py-8 rounded-full text-3xl hover:from-pink-600 hover:to-purple-600 transition duration-300 transform hover:scale-105"
          >体験予約</a
        >
      </div>
    </section>

    <!-- ビジョンセクション（創造の鼓動） -->
    <section id="vision" class="relative h-screen flex items-center justify-center">
      <Picture
        src={aboutImage}
        formats={['jpg']}
        alt="BeatMove Vision Background"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-black opacity-50"></div>
      <div class="container mx-auto px-6 relative z-10 text-center">
        <h2 class="text-7xl font-bold mb-12">創造の鼓動</h2>
        <p class="text-3xl mb-8">
          消費から創造へ。BeatMoveは、運動と音楽制作の融合により、<br />新たな自己表現の扉を開きます。
        </p>
        <p class="text-3xl mb-8">
          <a href="https://www.b-monster.jp/" target="_blank" rel="noopener noreferrer">b-monster</a>や<a
            href="https://feelcycle.com/"
            target="_blank"
            rel="noopener noreferrer">FEELCYCLE</a
          >が切り拓いた道を更に前進させ、<br />BeatMoveは運を通じて、あなた自身のサウンドスケープを生み出します。
        </p>
        <p class="text-3xl mb-16">
          創造の喜びが、あなたと周りの世界を変える。<br />その可能性を、我々は追求し続けます。
        </p>
      </div>
    </section>

    <!-- Modern Marquee for sync5.svg -->
    <div class="gradient-bg py-16 overflow-hidden">
      <div class="container mx-auto px-6 relative">
        <div class="absolute inset-0 flex items-center justify-center opacity-10">
          <div class="w-64 h-64 bg-pink-500 rounded-full filter blur-3xl"></div>
        </div>
        <div class="relative z-10">
          <h2 class="text-5xl font-bold mb-8 text-center text-white">BeatSync Technology</h2>
          <div class="flex justify-center">
            <div
              class="bg-gray-900 p-8 rounded-lg shadow-2xl max-w-3xl transform hover:scale-105 transition duration-300"
            >
              <Image src={syncSvg} alt="Beat Sync Magic" class="w-full h-auto" />
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 革新セクション（フィットネスの革命） -->
    <section id="revolution" class="relative min-h-screen flex flex-col items-center justify-center py-20">
      <Picture
        src={persona1Image}
        formats={['jpg']}
        alt="BeatMove Revolution Background"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-black opacity-50"></div>
      <div class="container mx-auto px-6 relative z-10 text-center">
        <h2 class="text-7xl font-bold mb-12">フィットネスの革命</h2>
        <p class="text-3xl mb-8">心拍がビートを刻む。汗が音符を描く。</p>
        <p class="text-3xl mb-8">あなたの動きが、世界にない音楽を生み出す。</p>
        <p class="text-3xl mb-16">BeatMoveで、フィットネスを芸術に昇華させる。</p>
      </div>
    </section>

    <!-- BeatMoveとは -->
    <section class="py-20">
      <div class="container mx-auto px-6">
        <h2 class="text-5xl font-bold mb-12 text-center">BeatMove - 三つの革新</h2>
        <div class="space-y-32">
          <!-- 一般ユーザー向け -->
          <div class="relative h-screen flex items-center justify-center">
            <Picture
              src={persona2Image}
              formats={['jpg']}
              alt="New Self Discovery Background"
              widths={[1024, 1920, 2560]}
              sizes="100vw"
              class="absolute inset-0 w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-black opacity-50"></div>
            <div class="container mx-auto px-6 relative z-10 text-center">
              <h3 class="text-6xl font-bold mb-8">感動を生み出す身体</h3>
              <p class="text-2xl mb-8">
                日々トレーニングに、創造の歓びを。BeatMoveは、運動と音楽制作の融合により、全く新しいフィットネス体験を実現します。
              </p>
              <p class="text-2xl mb-12">
                心拍とシンクロするビートが、身体と音楽の一体感を生み出します。専門知識不要の直感的操作で、あなただけの音楽が誕生。単なる運動から、創造的な自己表現の時間へ。
              </p>
              <a
                href="#contact"
                class="bg-pink-500 text-white px-12 py-6 rounded-full text-2xl hover:bg-pink-600 transition duration-300"
                >体験予約</a
              >
            </div>
          </div>

          <!-- ジム経営者向け -->
          <div class="relative h-screen flex items-center justify-center">
            <Picture
              src={persona3Image}
              formats={['jpg']}
              alt="Gym Innovation Background"
              widths={[1024, 1920, 2560]}
              sizes="100vw"
              class="absolute inset-0 w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-black opacity-50"></div>
            <div class="container mx-auto px-6 relative z-10 text-center">
              <h3 class="text-6xl font-bold mb-8">ジムに革新をもたらす</h3>
              <p class="text-2xl mb-8">
                フィットネス業界に新たな風を。BeatMoveは、最先端技術と独自コンセプトにより、他にはない体験を提します。
              </p>
              <p class="text-2xl mb-12">
                法人向けライセンスプログラムで、簡単導入。顧客満足度とリピート率の向上を実現し、進化するフィットネス市場の先駆者となる機会を提供します。
              </p>
              <a
                href="#contact"
                class="bg-pink-500 text-white px-12 py-6 rounded-full text-2xl hover:bg-pink-600 transition duration-300"
                >詳細資料請求</a
              >
            </div>
          </div>

          <!-- フランチャイズパートナー向け -->
          <div class="relative h-screen flex items-center justify-center">
            <Picture
              src={persona4Image}
              formats={['jpg']}
              alt="Future Fitness Partner Background"
              widths={[1024, 1920, 2560]}
              sizes="100vw"
              class="absolute inset-0 w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-black opacity-50"></div>
            <div class="container mx-auto px-6 relative z-10 text-center">
              <h3 class="text-6xl font-bold mb-8">未来のフィットネスを共に</h3>
              <p class="text-2xl mb-8">
                新たなビジネスフロンティアを求めて。BeatMoveは、運動と音楽制作を融合させた革新的なスタジオ型ジムのコンセプトを提供します。
              </p>
              <p class="text-2xl mb-12">
                特許出願中の独自システムで、未開拓の市場を共に切り拓く。創造性とフィットネスの融合で、業界のパオニアとなるチャンス。
              </p>
              <a
                href="#contact"
                class="bg-pink-500 text-white px-12 py-6 rounded-full text-2xl hover:bg-pink-600 transition duration-300"
                >パートナー申込</a
              >
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Ableton Moveセクション -->
    <section class="py-20 relative">
      <Picture
        src={tourImage12}
        formats={['jpg']}
        alt="Ableton Move Background"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-black opacity-70"></div>
      <div class="container mx-auto px-6 text-center relative z-10">
        <h2 class="text-6xl font-bold mb-12">Ableton Moveとの連携</h2>
        <p class="text-2xl mb-8">
          BeatMove（ビートムーブ）は、2024年10月8日に発売された「Ableton
          Move」と連携し、より直感的でパワフル音楽制作を可能にします。
        </p>
        <p class="text-2xl mb-8">
          「Ableton
          Move」は、シンセサイザー、サンプラー、エフェクター、シーケンサーといった機能を網羅したオール・イン・ワンの音楽制作デバイスです。横幅313.5mm、重量970グラムと軽量・コンパクトで、最大4時間使用できるバッテリーを搭載。USB-Cポートにより高速なデータ転送と充電が可能です。
        </p>
        <p class="text-2xl mb-8">
          ステレオ・スピーカーやサンプリング用マイク内蔵しており、どこでも簡単に高品質な音楽制作が行えます。世界中のプロが愛用する「Ableton
          Live」の機能を、パソコンなしで手のひらに収めました。
        </p>
      </div>
    </section>

    <!-- Ableton Move ビデオセクション -->
    <section class="py-20 bg-gray-900">
      <div class="container mx-auto px-6">
        <h2 class="text-4xl font-bold mb-12 text-center">直感的な音楽制作のためのデバイス連携</h2>
        <div class="max-w-7xl mx-auto relative" style="padding-bottom: 56.25%;">
          <div
            id="videoOverlay"
            class="absolute inset-0 flex items-center justify-center bg-black bg-opacity-50 cursor-pointer"
          >
            <button
              id="playButton"
              class="bg-pink-500 text-white px-8 py-4 rounded-full text-2xl hover:bg-pink-600 transition duration-300"
            >
              動画を再生
            </button>
          </div>
          <iframe
            id="abletonVideo"
            src="https://www.youtube.com/embed/qg0BzsVCMGs?enablejsapi=1"
            frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            class="absolute top-0 left-0 w-full h-full"></iframe>
        </div>
      </div>
    </section>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const video = document.getElementById('abletonVideo') as HTMLIFrameElement;
        const overlay = document.getElementById('videoOverlay');
        const playButton = document.getElementById('playButton');

        if (video && overlay && playButton) {
          playButton.addEventListener('click', () => {
            video.contentWindow?.postMessage('{"event":"command","func":"playVideo","args":""}', '*');
            overlay.style.display = 'none';

            // Request fullscreen
            if (video.requestFullscreen) {
              video.requestFullscreen().catch((err) => {
                console.error(`Error attempting to enable fullscreen: ${err.message}`);
              });
            }
          });
        }
      });
    </script>

    <!-- 特許セクション（更新） -->
    <section class="py-32 relative min-h-screen flex items-center">
      <Picture
        src={beatmoveActualImage}
        formats={['jpg']}
        alt="BeatMove Actual Usage"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover object-center"
      />
      <div class="absolute inset-0 bg-black opacity-70"></div>
      <div class="container mx-auto px-6 text-center relative z-10">
        <h2 class="text-6xl font-bold mb-16">特許出願中の革新的技術</h2>
        <p class="text-2xl mb-12">BeatMoveは、以下の独自技術により、フィットネス体験に革命をもたらします。</p>
        <div class="md:flex md:space-x-8 mt-16">
          <div class="md:w-1/2 mb-12 md:mb-0">
            <div
              class="bg-gray-900 bg-opacity-90 p-8 rounded-lg shadow-2xl transform hover:scale-105 transition duration-300"
            >
              <iconify-icon icon="mdi:music-note-bluetooth" width="64" height="64" class="text-pink-500 mb-6"
              ></iconify-icon>
              <h3 class="text-3xl font-bold mb-6">運動同期音楽生成コンセプト</h3>
              <p class="text-xl">
                お客様自身が運動に合わせて音楽を生成できる革新的なシステム。 心拍数と音楽のBPMを同期させることで、
                かつてない没入感を実現します。
              </p>
            </div>
          </div>
          <div class="md:w-1/2">
            <div
              class="bg-gray-900 bg-opacity-90 p-8 rounded-lg shadow-2xl transform hover:scale-105 transition duration-300"
            >
              <iconify-icon icon="mdi:playlist-music" width="64" height="64" class="text-pink-500 mb-6"></iconify-icon>
              <h3 class="text-3xl font-bold mb-6">プロフェッショナル楽曲更新システム</h3>
              <p class="text-xl">
                毎月、プロの音楽家が制作する32種類の最新ベース楽曲を提供。 常に新鮮で、ユーザーの好みに合わせた
                音楽体験を実現します。
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 体験セクション（更新） -->
    <section class="relative min-h-screen flex items-center justify-center">
      <Picture
        src={experienceImage}
        formats={['png', 'webp', 'avif']}
        alt="BeatMove Experience"
        widths={[1024, 1920, 2560]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover"
      />
      <div class="absolute inset-0 bg-black opacity-50"></div>
      <div class="container mx-auto px-6 text-center relative z-10">
        <h2 class="text-5xl font-bold mb-12">BeatMoveを体験する</h2>
        <p class="text-2xl mb-6">経営者の皆様、BeatMoveの革新的なフィットネス体験を直接お試しいただけます。</p>
        <p class="text-2xl mb-12">
          目黒区にある当社提携ジムにて、実際にBeatMoveをご体験いただけます。最新のテクノロジーと音楽が融合した、新しいフィットネスの形をぜひ肌で感じてください。
        </p>
        <div class="mt-8">
          <a
            href="#contact"
            class="bg-pink-500 text-white px-8 py-4 rounded-full text-xl hover:bg-pink-600 transition duration-300"
            >体験予約はこちら</a
          >
        </div>
      </div>
    </section>

    <!-- お問い合わせセクション -->
    <section id="contact" class="py-20 bg-gray-800">
      <div class="container mx-auto px-6 text-center">
        <h2 class="text-5xl font-bold mb-8">お問い合わせ・事前登録</h2>
        <p class="text-xl mb-12">以下のフォームより、お気軽にお問い合わせください。</p>
        <!-- 問い合わせフォーム -->
        <div class="max-w-lg mx-auto">
          <form action="#" method="post" class="space-y-6">
            <div>
              <label for="name" class="block mb-2 text-lg">お名前</label>
              <input type="text" id="name" name="name" class="w-full p-3 bg-gray-800 rounded-lg" required />
            </div>
            <div>
              <label for="email" class="block mb-2 text-lg">メールアドレス</label>
              <input type="email" id="email" name="email" class="w-full p-3 bg-gray-800 rounded-lg" required />
            </div>
            <div>
              <label for="message" class="block mb-2 text-lg">お問い合わせ内容</label>
              <textarea id="message" name="message" rows="5" class="w-full p-3 bg-gray-800 rounded-lg" required
              ></textarea>
            </div>
            <div class="text-center">
              <button type="submit" class="bg-pink-500 text-white px-6 py-3 rounded-full text-lg hover:bg-pink-600"
                >送信する</button
              >
            </div>
          </form>
        </div>
      </div>
    </section>

    <!-- フッター -->
    <footer class="bg-gray-800">
      <div class="container mx-auto px-6 py-12 text-center">
        <p class="mb-4">
          &copy; 2024 <a
            href="https://g-productions.studio"
            class="text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-purple-500 hover:from-pink-600 hover:to-purple-600"
            >G Productions</a
          >
        </p>
        <p class="mb-4">Email: beatmove@gproductions.co.jp</p>
        <p>住所: 〒153-0042 東京都目黒区青葉台4丁目4番12号</p>
      </div>
    </footer>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const header = document.querySelector('header');
        const pageOverlay = document.getElementById('pageOverlay');
        if (header && pageOverlay) {
          let lastScrollY = window.scrollY;
          let ticking = false;

          const updateHeaderAndOverlay = () => {
            if (lastScrollY < window.scrollY) {
              header.style.transform = 'translateY(-100%)';
            } else {
              header.style.transform = 'translateY(0)';
            }

            // Update overlay opacity based on scroll position
            const scrollPercentage = Math.min(
              window.scrollY / (document.documentElement.scrollHeight - window.innerHeight),
              1
            );
            pageOverlay.style.opacity = (scrollPercentage * 0.7).toFixed(2); // Adjust max opacity

            lastScrollY = window.scrollY;
            ticking = false;
          };

          window.addEventListener('scroll', () => {
            if (!ticking) {
              window.requestAnimationFrame(() => {
                updateHeaderAndOverlay();
                ticking = false;
              });
              ticking = true;
            }
          });
        }
      });
    </script>
  </body>
</html>
